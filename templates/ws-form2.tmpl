{{define "body"}}
	<body>
        {{ template "ws" . }}
		<div class="container">
			<h1>&#127821; Metadata Hub</h1>
			<h2>Working...</h2>
            {{ . }}
			<div id="countfeedback"></div>
			<div id="processcomplete"></div>

	</body>
{{end}}